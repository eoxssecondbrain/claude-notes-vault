---
thread_name: "gmail-spam-scan-2026-08-31"
user: "raj"
type: claude-chat
created: 2026-08-31
updated: 2026-08-31
---

USER (scheduled task prompt):
# Scheduled Email Spam Detection (v3)

At each scheduled run, call `list_labels` first to get label IDs; create any missing label with `create_label` (AI-SPAM, AI-SPAM/Advertising, AI-SPAM/Expired-OTP, AI-SPAM/Fraud, AI-Reviewed).

**Step 0 — Repair pass (run this FIRST, every single firing, before anything else):**
Search `label:AI-SPAM -in:spam` (pass `includeTrash: true`) to find any thread that was already labeled as spam/suspicious by a past run but never actually got moved into the Spam folder. This is a known failure mode: `label_thread` can succeed while a later `mark_thread_spam` call for the same thread doesn't complete (the run gets interrupted, hits a turn/budget limit, or errors) — and because the normal-run search below permanently excludes anything already carrying the AI-SPAM label, an orphaned thread like this is never revisited by any future run unless this repair pass catches it. Call `mark_thread_spam` on every thread this search returns, paginating with pageToken until it returns none. This step requires no re-classification (the thread is already labeled) — just complete the move. Record how many orphans were found and fixed; report this count at the end of the run.

**Determining run size (Step 1):**
- If the AI-Reviewed and AI-SPAM labels do not yet exist, or neither has ever been applied to any thread (i.e. this is the first run ever), treat this as the FIRST RUN: call `search_threads` with query `in:inbox` sorted newest-first, pageSize 100, and process at most the 100 most recent inbox emails (paginate with pageToken only up to that 100 cap).
- Otherwise (a normal run): call `search_threads` with query `in:inbox -label:AI-SPAM -label:AI-Reviewed`, pageSize 50, and process what comes back, paginating with pageToken if more than 50 unprocessed emails exist (this covers a gap after downtime or a backlog).

Call `get_thread` with `messageFormat PLAIN_TEXT` when the body needs to be read.

For every email in the run:

1. Read sender, subject, and body.
2. Check for common spam/phishing indicators: suspicious or misleading links; requests for passwords, OTPs, banking details, or other sensitive information; a one-time code, verification code, PIN, launch code, or magic sign-in link that is more than 24 hours old (a spent credential — codes under 24 hours old are still usable and must not be flagged); fake prizes, offers, marketing emails, or urgent requests; impersonation of companies or individuals; unusual sender information; suspicious attachments or instructions.
3. Before classifying, skip these entirely — mark NOT_SPAM without further analysis, overriding every indicator in step 2: sender domain eoxs.com or eoxsteam.com, or any eoxs address in To/Cc; security notices; payment/invoice/receipt/subscription-charge mail; calendar mail; auto-replies and out-of-office; offer letters, contracts, onboarding forms, repository/system access invitations; any code or sign-in link less than 24 hours old; outbound sales/prospecting correspondence sent BY someone representing EOXS to a third-party prospect.
4. Classify as SPAM, SUSPICIOUS, or NOT_SPAM.
5. If SPAM or SUSPICIOUS: label_thread with AI-SPAM + sub-label, then immediately mark_thread_spam on that same thread, atomically per thread.
6. If NOT_SPAM: apply the AI-Reviewed label and nothing else.
7. Do not modify or delete email content itself.
8. Do not process the same email twice.
9. At the end of the task, report: how many orphans Step 0 found and fixed, number checked in Step 1, and counts of SPAM, SUSPICIOUS, NOT_SPAM, plus how many were moved to Spam.

ASSISTANT (my actions and findings):
Loaded the threads-ov skill first per standing instructions (mandatory save_chat_transcript rule).

Called list_labels: all five required labels already exist (AI-SPAM=Label_33, AI-SPAM/Advertising=Label_34, AI-SPAM/Expired-OTP=Label_35, AI-SPAM/Fraud=Label_36, AI-Reviewed=Label_37). AI-Reviewed has 19,689 threads total, so this is NOT the first run — treated as a normal run.

Step 0 (repair pass): searched `label:AI-SPAM -in:spam` with includeTrash:true. Zero results. No orphaned threads found — nothing to fix.

Step 1 (normal run): searched `in:inbox -label:AI-SPAM -label:AI-Reviewed`, pageSize 50. resultCountEstimate was 1 — a single thread, id 1a04998c048a0602, subject "Re: S00208", sender info.eoxs@gmail.com, to rajat@eoxs.com. This thread's first message already carried AI-Reviewed (Label_37), but a newer reply in the same thread (dated 2026-08-31 12:24:06Z) had not yet been labeled, which is why the thread still matched the unprocessed-thread query.

Read the full thread via get_thread (PLAIN_TEXT). Both messages are internal EOXS system notifications from the Odoo/Teams platform (teams.eoxs.com) — "View Sales Order" / Sales Order S00208 approval notification, addressed to rajat@eoxs.com, links point to teams.eoxs.com. No spam indicators; matches the skip-list (EOXS internal system notification to an eoxs.com address). Classified NOT_SPAM.

Applied label_thread with AI-Reviewed (Label_37) to the thread (re-covers the new message).

No other threads required processing.

Final report:
- Step 0 orphans found and fixed: 0
- Step 1 threads checked: 1
- SPAM: 0
- SUSPICIOUS: 0
- NOT_SPAM: 1
- Moved to Spam: 0

Run was clean — no spam or suspicious mail found, no orphaned threads, inbox is caught up. No user notification sent since nothing actionable was found.
