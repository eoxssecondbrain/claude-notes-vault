__pycache__/
*.pyc
.env
.venv/

# Local working copy of OV2's repo, used only by propose_ov2_xref/apply_ov2_xref.
# Must never be committed here — it's OV2's content, not this vault's.
.ov2-clone/
.ov2-xref-staging/

# Pending user secrets awaiting assignment to real names — credential material,
# must never be committed. See mcp_server.py's CLAUDE_OV_USERS.
.pending-users-DO-NOT-COMMIT.json
.pending-users-DO-NOT-COMMIT.md
